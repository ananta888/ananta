{
  "name": "@ananta/game-core",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic, framework-independent Ananta hex game prototype",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.cjs"
  },
  "devDependencies": { "typescript": "5.9.3" }
}
