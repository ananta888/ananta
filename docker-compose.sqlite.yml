# Docker-Compose für Leichtgewicht-Umgebung mit SQLite
# Basiert auf docker-compose.base.yml
# Nutzung: docker compose -f docker-compose.base.yml -f docker-compose.sqlite.yml up

services:
  # Keine zusätzlichen Services wie Postgres/Redis nötig für SQLite Mode
  # Die Basis-Konfiguration nutzt standardmäßig SQLite, wenn DATABASE_URL nicht gesetzt ist.

  ai-agent-hub: {}

  ai-agent-alpha: {}

  ai-agent-beta: {}
