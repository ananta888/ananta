# Input for the hash-locked optional worker dependency set. Regenerate the
# adjacent lock with the command embedded in its header; images install only
# the lock file, never this unconstrained transitive input.
dspy==3.2.1
