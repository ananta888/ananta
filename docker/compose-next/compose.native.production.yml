# Native runtime selection. The shared production credential boundary lives in
# compose.workflow-runtime.production.yml and must be loaded immediately before
# this runtime-specific overlay.
services:
  ai-agent-hub:
    environment:
      ANANTA_ORCHESTRATION_BACKEND: local
      ANANTA_WORKFLOW_BACKEND: local
