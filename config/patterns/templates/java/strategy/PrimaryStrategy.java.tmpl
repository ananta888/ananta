package @@package_name@@;

import java.util.ArrayList;
import java.util.Collections;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

/**
 * Primary strategy: lower-cases and sorts the input items.
 */
public class Primary@@context_class@@Strategy implements @@context_class@@Strategy {

    @Override
    public Map<String, Object> execute(Map<String, Object> payload) {
        Map<String, Object> result = new HashMap<>();
        result.put("variant", "primary");
        List<String> items = new ArrayList<>();
        Object raw = payload == null ? null : payload.get("items");
        if (raw instanceof Iterable) {
            for (Object o : (Iterable<?>) raw) {
                items.add(String.valueOf(o).toLowerCase());
            }
        }
        Collections.sort(items);
        result.put("items", items);
        return result;
    }
}
