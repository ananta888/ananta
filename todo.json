[
  {
    "task": "Update .gitignore to include root-level data and log files explicitly (e.g., /data/ananta.db, /app.log, /audit.log) to ensure consistency across different deployment environments.",
    "status": "open"
  }
]
