# LangExtract claim adapter

LangExtract is an optional worker strategy behind `ClaimExtractionPort`.
The fixture adapter is the CI default. Grounded spans prove only that
extracted text occurs in the delegated source, not that it is true.
Hub admission remains fail-closed.
