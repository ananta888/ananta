# Local MoE / root-cause routing

Laguna-style ideas are adopted as **policies**, not as a model lock-in.
`root_cause_investigation` tasks get a hard thinking/tool budget and
must keep conflicting evidence visible in a ConflictSet. MoE offload is
advisory and off by default.
