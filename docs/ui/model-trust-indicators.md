# Model trust indicators

The existing model inventory projects `trust_class` and `safety_modified` as
declared facts. Clients must show both next to provider, model, revision,
quantization, hardware and evidence state. `unsafe_research` must never be
phrased as better, free or safe. A blocked route explains the policy reason but
does not offer a bypass.
