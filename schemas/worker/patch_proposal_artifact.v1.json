{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ananta.local/schemas/worker/patch_proposal_artifact.v1.json",
  "title": "Ananta PatchProposalArtifact v1",
  "description": "Non-executable patch proposal with unified diff hunks. Requires validation/approval before apply.",
  "type": "object",
  "required": ["schema", "proposal_id", "goal_id", "task_id", "strategy_id", "patches"],
  "properties": {
    "schema": { "type": "string", "const": "patch_proposal_artifact.v1" },
    "proposal_id": { "type": "string", "minLength": 1 },
    "goal_id": { "type": "string", "minLength": 1 },
    "task_id": { "type": "string", "minLength": 1 },
    "strategy_id": { "type": "string", "minLength": 1 },
    "patches": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["path", "content"],
        "properties": {
          "path": { "type": "string" },
          "content": { "type": "string" },
          "hunk_id": { "type": "string" }
        }
      }
    },
    "expected_artifacts": {
      "type": "array",
      "items": { "type": "object" },
      "default": []
    },
    "reason": { "type": ["string", "null"] },
    "created_at": { "type": "number" },
    "metadata": { "type": "object", "default": {} }
  },
  "additionalProperties": false
}