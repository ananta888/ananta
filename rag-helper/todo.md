Priorität 1 – direkt wichtig
1. ADOC-Unterstützung ergänzen
   *.adoc einlesen
   in semantische Abschnitte zerlegen
   erfassen:
   Dokumenttitel
   Abschnittspfad
   Überschrift
   Textinhalt
   Listen
   Codeblöcke
   Ausgabe als:
   adoc_section
   optional adoc_code_block
2. Include-/Exclude-Filter einbauen
   --include-glob
   --exclude-glob
   typische Excludes:
   target/**
   build/**
   generated/**
   node_modules/**
   große Export-/Dump-Dateien
3. Größenlimits für große Dateien
   --max-file-size-kb
   --max-xml-nodes
   --max-methods-per-class
   --max-records-per-file
4. XML selektiv behandeln
   Konfig/XML anders behandeln als Daten/XML
   Heuristiken:
   Spring/XML-Config erkennen
   Mapper/XML erkennen
   sehr repetitive Daten-XML überspringen
   optional:
   --xml-mode config-only|all|smart
5. Incremental / Cache
   Hash pro Datei speichern
   nur geänderte Dateien neu parsen
   Cache-Datei:
   .code_to_rag_cache.json
   Optionen:
   --incremental
   --rebuild
   Priorität 2 – Qualität des Outputs
6. Generierten Code erkennen
   JAXB
   Lombok-generated Patterns
   generated-sources
   Kommentar-Heuristiken wie:
   Generated by
   Do not edit
   markieren oder optional ausschließen
7. Bessere Typauflösung
   Wildcard-Imports teilweise auflösen
   inner classes besser behandeln
   gleiche Package-Struktur besser matchen
   Import-Konflikte markieren
8. Method-Call-Auflösung verbessern
   nicht nur calls, sondern grobe Zielheuristik
   z. B.:
   repo.save(...) → wahrscheinliche Zielklasse UserRepository
   Feldname → Feldtyp → Methodenaufruf ableiten
9. Spring-/JPA-Heuristiken ergänzen
   @Autowired
   @Inject
   @Bean
   @Configuration
   @Transactional
   @OneToMany, @ManyToOne, @JoinColumn
   daraus Relationen erzeugen
10. Bessere Rollenklassifikation
    config
    mapper
    adapter
    client
    facade
    util
    exception
    enum-model
    Priorität 3 – RAG-Nutzen verbessern
11. Embedding-Text optimieren
    kompakter
    weniger Wiederholung
    einheitliche Reihenfolge
    separate Modi:
    verbose
    compact
12. Separate Retrieval-Ebenen
    index.jsonl → nur Embeddings
    details.jsonl → Nachlade-Kontext
    relations.jsonl → Graph / Zusatzretrieval
    optional:
    embedding.jsonl
    context.jsonl
13. Wichtigkeit / Score pro Record
    Score für:
    Controller
    Service
    fachliche XSD-Typen
    ADOC-Architekturabschnitte
    so kann Retrieval priorisieren
14. Parent-/Child-Links vereinheitlichen
    Datei → Typ
    Typ → Methode
    XSD → Element
    ADOC → Abschnitt
    IDs konsistent machen
15. Manifest erweitern
    Record-Zahlen pro Typ
    Fehlerliste
    Skip-Gründe
    Cache-Treffer
    Statistik je Endung
    Priorität 4 – Performance / Betrieb
16. Parallelisierung
    Dateien parallel verarbeiten
    sinnvoll für großes Projekt
    z. B. concurrent.futures
17. Fortschrittsanzeige
    verarbeitete Dateien
    Prozent
    aktuelle Datei
    Skip-/Error-Zähler
18. Fehlerrobustheit erhöhen
    Parserfehler isolieren
    fehlerhafte Datei nicht Gesamtprozess stoppen lassen
    Fehler-Log schreiben
19. Resume-Modus
    nach Abbruch weiterlaufen
    nur fehlende/geänderte Dateien neu bauen
20. CLI-Konfiguration per JSON/YAML
    statt alles nur per Argumente
    z. B. Projektprofil:
    welche Dateien
    welche Filter
    welche Limits
    Priorität 5 – später nice to have
21. Graph-Export
    graph_nodes.jsonl
    graph_edges.jsonl
    optional Neo4j-kompatibel
22. Properties/YAML/SQL/MD ergänzen
    properties
    yaml
    yml
    sql
    md
23. Duplicate-/Boilerplate-Erkennung
    ähnliche Klassen
    sehr ähnliche XML-Strukturen
    redundante DTOs
24. Domain-spezifische Chunker
    Spring XML
    Maven POM
    XSD
    AsciiDoc
    JPA Entity
25. Profiling / Benchmark-Modus
    Laufzeit pro Dateityp
    größter Output
    problematische Dateien
    Meine empfohlene Reihenfolge

Für dein Projekt würde ich so vorgehen:

ADOC-Support
Include/Exclude-Glob
Dateigrößen- und XML-Limits
Incremental Cache
generierten Code filtern
Spring/JPA-Heuristiken
Call-Resolution light
Parallelisierung
Graph-Export
weitere Dateitypen
Kurzfassung als kompakte Todo-Liste
[ ] ADOC parser + section chunks
[ ] include/exclude glob support
[ ] max file size / node / record limits
[ ] smart XML filtering
[ ] incremental cache by file hash
[ ] generated code detection
[ ] improved Java type resolution
[ ] lightweight method target resolution
[ ] Spring/JPA annotation relations
[ ] better role classification
[ ] optimized embedding_text modes
[ ] split retrieval/context outputs
[ ] importance scoring
[ ] consistent parent/child links
[ ] richer manifest stats
[ ] parallel processing
[ ] progress output
[ ] stronger error handling
[ ] resume mode
[ ] config file support
[ ] graph export
[ ] support properties/yaml/sql/md
[ ] duplicate detection
[ ] specialized chunkers
[ ] benchmark mode

Wenn du willst, formatiere ich dir das direkt noch als Markdown-Taskliste für eine README oder als JSON/YAML-Roadmap-Konfiguration.
