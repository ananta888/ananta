# code_interpreter

```json
{
  "code": "from fastapi import FastAPI, HTTPException\nfrom pydantic import BaseModel\nfrom typing import Optional\n\n# Initialize FastAPI app\napp = FastAPI(\n    title=\"Fibonacci Service\",\n    description=\"Provides basic Fibonacci number calculation.\",\n    version=\"0.1.0\"\n)\n\n# Pydantic Model for the response (optional but good practice)\nclass FibonacciResponse(BaseModel):\n    n: int\n    fibonacci_number: int\n\n# --- Service Layer / Core Logic ---\ndef calculate_fibonacci(n: int) -> int:\n    \"\"\"\n    Berechnet das N-te Fibonacci-Zahl rekursiv.\n    Wir implementieren hier die Basislogik ohne Optimierungen (Memoization, etc.).\n    \"\"\"\n    if n < 0:\n        raise ValueError(\"Fibonacci number is not defined for negative input.\")\n    if n == 0:\n        return 0\n    elif n == 1:\n        return 1\n    else:\n        # Reine Rekursion für die Basis-Implementierung\n        return calculate_fibonacci(n - 1) + calculate_fibonacci(n - 2)\n\n# --- FastAPI Endpoint Definition ---\n\n@app.get(\n    \"/fib/{n}\",\n    response_model=FibonacciResponse,\n    summary=\"Berechnet die N-te Fibonacci-Zahl\",\n    responses={\n        200: {\"description\": \"Die berechnete Fibonacci-Zahl.\"},\n        422: {\"description\": \"Ungültige Eingabe (z.B. negative Zahl).\"},\n    }\n)\nasync def get_fibonacci(n: int):\n    \"\"\"\n    Nimmt eine nicht-negative Integer N als URL-Parameter entgegen und gibt F(N) zurück.\n    \"\"\"\n    try:\n        # Hier wird die reine Berechnung aufgerufen\n        result = calculate_fibonacci(n)\n        return FibonacciResponse(n=n, fibonacci_number=result)\n    except ValueError as e:\n        # Obwohl FastAPI bereits gewisse Validierungen macht, fangen wir explizit ab, \n        # wenn die Kernlogik einen Fehler wirft (hier: negative Zahlen).\n        raise HTTPException(status_code=422, detail=str(e))\n\n# Optional: Code zur Überprüfung des Services (falls benötigt)\nif __name__ == \"__main__\":\n    import uvicorn\n    # Startet den Uvicorn Web-Server\n    uvicorn.run(app, host=\"0.0.0.0\", port=8000)"
}
```
