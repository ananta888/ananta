# code_interpreter

```json
{
  "code": "from fastapi import FastAPI, HTTPException, Body, Depends\nfrom pydantic import BaseModel, Field, validator\nfrom typing import Optional\nimport uuid\n\n# --- Mock Database/Storage ---\n# Simulating a database for links. Structure: {short_code: {\"long_url\": \"...\", \"created_by\": \"...\"}}\nLINK_DATABASE = {}\n\n# --- Pydantic Models for Validation ---\n\nclass LinkCreateRequest(BaseModel):\n    \"\"\"Defines the expected payload for creating a link.\"\"\"\n    long_url: str = Field(..., description=\"The full, target URL for the link.\")\n    owner_email: str = Field(..., description=\"Email of the link creator for tracking.\")\n\n    @validator('long_url')\n    def validate_url_format(cls, v):\n        \"\"\"Basic validation to check if the URL looks reasonable.\"\"\"\n        if not v.startswith(('http://', 'https://')):\n            raise ValueError('The provided long_url must start with http:// or https://')\n        return v\n\n    @validator('owner_email')\n    def validate_email_format(cls, v):\n        \"\"\"Basic email format validation.\"\"\"\n        if \"@\" not in v or \".\" not in v:\n            raise ValueError('Invalid email format.')\n        return v\n\nclass LinkResponse(BaseModel):\n    \"\"\"Defines the response structure for a successful link creation.\"\"\"\n    short_code: str\n    long_url: str\n    owner_email: str\n    created_at: str\n\n# --- FastAPI App Setup ---\napp = FastAPI(\n    title=\"Link Service API\",\n    description=\"Endpoints for creating, resolving, and managing short links.\",\n    version=\"1.0.0\"\n)\n\n# --- API Endpoints ---\n\n@app.post(\"/links/create\", response_model=LinkResponse, status_code=201)\ndef create_link(request: LinkCreateRequest):\n    \"\"\"\n    Erstellt einen neuen ShortLink basierend auf der langen URL.\n    Führt grundlegende Validierungen für URL und E-Mail durch.\n    \"\"\"\n    # 1. Simulate Short Code Generation\n    # Using a unique identifier for the mock implementation\n    new_short_code = str(uuid.uuid4())[:8]\n    \n    # 2. Check for existing collision (highly unlikely with UUID[:8], but good practice)\n    if new_short_code in LINK_DATABASE:\n        raise HTTPException(status_code=409, detail=\"Der generierte ShortCode ist bereits vergeben.\")\n\n    # 3. Store the link (Validation happened via Pydantic/Pydantic Validators)\n    global LINK_DATABASE\n    LINK_DATABASE[new_short_code] = {\n        \"long_url\": request.long_url,\n        \"owner_email\": request.owner_email\n    }\n    \n    # 4. Return the successful response\n    return LinkResponse(\n        short_code=new_short_code,\n        long_url=request.long_url,\n        owner_email=request.owner_email,\n        created_at=\"[Current Timestamp]\" # In a real app, use datetime.now()\n    )\n\n@app.get(\"/links/resolve/{short_code}\")\ndef resolve_link(short_code: str):\n    \"\"\"\n    Löst einen ShortCode auf und gibt die vollständige URL zurück.\n    Führt Validierung für das Vorhandensein des Codes durch.\n    \"\"\"\n    # 1. Input Validation: Check if short_code is provided and valid (FastAPI path parameter handles basic string checks)\n    if not short_code or not short_code.isalnum():\n         raise HTTPException(status_code=400, detail=\"Ungültiges Format für den ShortCode.\")\n\n    # 2. Lookup in Database\n    global LINK_DATABASE\n    if short_code not in LINK_DATABASE:\n        raise HTTPException(status_code=404, detail=f\"Der Link mit dem Code '{short_code}' wurde nicht gefunden.\")\n\n    # 3. Success: Return the full URL\n    resolved_url = LINK_DATABASE[short_code][\"long_url\"]\n    return {\n        \"status\": \"success\",\n        \"short_code\": short_code,\n        \"resolved_url\": resolved_url\n    }\n\n# --- Basic Root Endpoint for Health Check / Documentation ---\n@app.get(\"/\")\ndef read_root():\n    \"\"\"Health check endpoint.\"\"\"\n    return {\"message\": \"Link Service API is running.\"}\n\n# Example usage instructions (for the user)\nprint(\"--- API Setup Complete ---\")\nprint(\"The application is built using FastAPI.\")\nprint(\"To run this service, save the code as main.py and run: uvicorn main:app --reload\")\n\n# Example Endpoints Summary for the user:\nprint(\"\\n--- Endpoints Summary ---\")\nprint(f\"1. Erstellen eines Links: POST http://127.0.0.1:8000/links/create\")\nprint(\"   Body (JSON): { \\\"long_url\\\": \\\"https://example.com/target\\\", \\\"owner_email\\\": \\\"user@example.com\\\" }\")\nprint(\"   -> Validiert: URL-Format (muss mit http(s):// beginnen) und E-Mail-Format.\")\nprint(\"\\n2. Auflösen eines Links: GET http://127.0.0.1:8000/links/resolve/{short_code}\")\nprint(\"   Parameter: {short_code} (z.B. 'abcdefg1')\")\nprint(\"   -> Validiert: Existiert der Code im System? Gibt 404 bei Nicht-Existenz zurück.\")"
}
```
